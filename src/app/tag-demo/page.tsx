import { TagSystemDemo } from "@/components/examples/tag-system-demo";

/**
 * Página de demostración del sistema de etiquetas
 * Accesible en /tag-demo
 */
export default function TagDemoPage() {
  return <TagSystemDemo />;
}

export const metadata = {
  title: "Demo Sistema de Etiquetas | CalReact",
  description: "Demostración del sistema de etiquetas estilo Trello para CalReact",
};
